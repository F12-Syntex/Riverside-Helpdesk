'use client';

import QaApp from '../_components/QaApp';

/* The Q&A also answers at its old address, so existing links and the
   site index keep working; the front door is now "/". */

export default function HelpBotPage() {
  return <QaApp />;
}
